// Checkout page to display cart, order total ($), and take in fake payment info.
// Includes "Place Order" button (goes to OrderConfirmation page)
// Address and fake billing info is already populated from users table IF LOGGED IN

import React from "react";
import { useState, useEffect } from "react";
import { Link } from "react-router-dom";

// import any other dependent files here (ex. checkout)

function Checkout() {
  return <h1>This is the checkout page</h1>;
}

export default Checkout;
